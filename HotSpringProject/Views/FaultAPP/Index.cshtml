
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="wrapper wrapper-content animated fadeInRight">
    <div class="row">
        <div class="col-sm-12">
            <div class="ibox float-e-margins">
                <div class="ibox-content">
                    <div class="layui-card">
                            <div class="layui-card-body">
                                <form class="layui-form" action="" lay-filter="searchForm">
                                    <div class="layui-form-item">
                                        <div class="layui-form-item layui-inline">
                                            <div class="layui-input-inline">
                                                <input type="text" name="txtSName" placeholder="设备名称" class="layui-input">
                                            </div>
                                        </div>
                                        <div class="layui-form-item layui-inline">
                                            <button class="layui-btn" lay-submit="" lay-filter="btnSearch">
                                                <i class="layui-icon layui-icon-search"></i>
                                                查询
                                            </button>
                                            <button type="reset" class="layui-btn">
                                                <i class="layui-icon layui-icon-refresh"></i>
                                                重置
                                            </button>
                                        </div>
                                    </div>
                                </form>
                            </div>
                        </div>

                    <div class="table-responsive">

                        <table id="demo" lay-filter="test"></table>

                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<!--js模版表头工具栏-->
<script type="text/html" id="toolbarDemo">
    <div class="layui-btn-container">
        <button class="layui-btn layui-btn-sm" lay-event="add">添加</button>
    </div>
</script>
<!--操作单元格-->
<script type="text/html" id="barDemo">
    @*<a class="layui-btn layui-btn-xs" lay-event="edit">编辑</a>*@
    @*<a class="layui-btn layui-btn-danger layui-btn-xs" lay-event="del">删除</a>*@
    {{# if(d.show === 1){ }}
    <a class="layui-btn layui-btn-sm btn-report" lay-event="report">故障分析</a>
    <a class="layui-btn layui-btn-sm btn-report" lay-event="check">查看</a>
    {{# } }}
</script>
<script type="text/html" id="repair">
    
    {{# if (d.auditcount === d.count){ }}
    {{# if (d.end_count==0&&d.record_count==0){ }}

    <a class="layui-btn layui-btn-xs" lay-event="faultAnalyseAudit">审核</a>

    {{#  }else if(d.end_count>0&&d.record_count==0){ }}
    <a class="layui-btn layui-btn-xs" lay-event="faultAnalyseRecord">审核记录</a>
    <a class="layui-btn layui-btn-xs" lay-event="repairreport">维修上报</a>
    {{#  } else{ }}
    <a class="layui-btn layui-btn-xs" lay-event="faultAnalyseRecord">审核记录</a>
    <a class="layui-btn layui-btn-xs" lay-event="equRepairRecord">维修记录</a>
    {{# } }}
    {{# } }}
</script>
<script>
    layui.use(['table', 'form', 'util', 'layer'], function () {
        var table = layui.table;
        var util = layui.util;
        var layer = layui.layer;
        var form = layui.form;
        var $ = layui.jquery;
        
        //表格渲染
        table.render({
            elem: '#demo'
            , height: 525
            , url: '/faultapp/getrepairList' //数据接口
            , page: true//开启分页/
            , toolbar: '#toolbarDemo'
            , cols: [[ //表头
                { field: 'name', title: '设备名称', width:230}
                , { field: 'fault_report', title: '故障申报人' }
                , { field: 'fault_describe', title: '故障描述' }
                , { field: 'fault_time', title: '故障时间', templet: function (d) { return util.toDateString(d.create_time.replace("/Date(", "").replace(")/", ""), "yyyy-MM-dd") } }
                , { field: 'create_time', title: '创建时间', templet: function (d) { return util.toDateString(d.create_time.replace("/Date(", "").replace(")/", ""), "yyyy-MM-dd") } }
                , { title: '操作', toolbar: '#barDemo' }
                , { field: 'count', title: '审核状态',width:100, templet: function (d) { return d.auditcount + ' / ' + d.count; }}
                , { title: '维修任务', toolbar: '#repair',width:200 }
            ]]
            , parseData: function (res) { //res 即为原始返回的数据
                return {
                    "code": 0, //解析接口状态
                    "msg": res.message, //解析提示文本
                    "count": res.count, //解析数据长度
                    "data": res.data //解析数据列表
                };
            }
        });
        //行单击事件
        table.on('tool(test)', function (obj) {
            var data = obj.data;
            var id = obj.data.id;//申报id
            console.log(data)
            var eid = obj.data.equip_id
            if (obj.event === 'report') {
                if (document.cookie.split(';').some((item) => item.trim().startsWith('faultAnalyse=')))
                {
                   layer.msg('该分析已完成')
                } else {
                    layer.open({
                        title: '故障分析',
                        type: 2,
                        area: ['800px', '615px'],
                        //这里传申报表id
                        content: `/faultanalyse/detail?id=${id}&eid=${eid}`
                    })
                }
                       
                }
                if (obj.event === 'faultAnalyseAudit') {
                    layer.open({
                        title: '故障分析审核',
                        type: 2,
                        area: ['500px', '550px'],
                        content: '/faultapp/faultAnalyseAudit/' + id
                    })
                }
                if (obj.event === 'faultAnalyseRecord') {
                    layer.open({
                        title: '审核记录',
                        type: 2,
                        area: ['500px', '550px'],
                        content: '/faultapp/Record/' + id
                    })
                }
            if (obj.event === 'equRepairRecord') {
                layer.open({
                    title: '维修记录',
                    type: 2,
                    area: ['600px', '185px'],
                    content:'/repairrecord/index/'+id
                })
            }
            if (obj.event === 'repairreport') {
                layer.open({
                    title: '维修上报',
                    type: 2,
                    area: ['700px', '600px'],
                    content: `/repairtaskreport/truereport?id=${id}&equip_id=${eid}`
                })
            }
            if (obj.event === 'check') {
                layer.open({
                    title: '查看投票情况',
                    type: 2,
                    area: ['700px', '360px'],
                    content: `/faultanalyse/check/`+id
                })
            }
        });
        table.on('toolbar(test)', function (obj) {
            if (obj.event == "add") {
                layer.open({
                    title: '添加',
                    type: 2,
                    area: ['500px', '455px'],
                    content: '/faultapp/detail'
                })
            }
        })
        //搜索工具栏
        form.on('submit(btnSearch)', function (data) {
            //表格重载 where 再加一个参数name
            console.log(data)
            table.reload('demo', {
                where: {
                    name: data.field.txtSName,
                }
            })
            return false;
        });
    })
</script>

